# -*- coding: utf-8 -*-
"""
user_05_餐饮折扣_系统直连 · 自动化测试
共 29 条用例
来源模块: 餐饮折扣 （品牌会员共享模式一：系统直连）, 餐饮折扣-系统直连 (小程序环境)

运行方法:
  pytest admin-web/tests/generated\test_user_05_餐饮折扣_系统直连.py -v --headed     # 有界面
  pytest admin-web/tests/generated\test_user_05_餐饮折扣_系统直连.py -v              # 无头模式
  pytest admin-web/tests/generated\test_user_05_餐饮折扣_系统直连.py -k "test_fyxt"  # 只跑编号含fyxt的
"""
import pytest
import sys
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
import os
from utils.case_mapping import case


class Test餐饮折扣系统直连:
    """user_05_餐饮折扣_系统直连 (29条用例)"""

    @case("cyzkxtzl-xcx-001", title="验证小程序扫码支付全流程（wx.scanCode → 支付）", priority="P0")
    def test_cyzkxtzl_xcx_001(self, page):
        """[cyzkxtzl-xcx-001] 验证小程序扫码支付全流程（wx.scanCode → 支付）  [P0]"""
        # ── 前置条件 ──
        #   1. 商家已在小程序端生成收款二维码
    #   2. 用户打开快乐羊毛小程序
        #
        # ── 测试步骤 ──
        #   1. 用户在小程序内点击"扫码支付"。
    #   2. 小程序调起摄像头（首次弹授权）。
    #   3. 对准商家二维码扫描。
    #   4. 扫码成功，跳转到支付确认页。
    #   5. 确认原价/优惠/实付金额。
    #   6. 点击支付，微信支付弹窗弹出。
    #   7. 输入密码完成支付
        #
        # ── 预期结果 ──
        #   1. 扫码功能正常调起（wx.scanCode）。
    #   2. 首次使用弹出摄像头授权，允许后正常扫描。
    #   3. 扫码解析二维码内容并跳转正确页面。
    #   4. 支付确认页展示正确的金额（如原价100/优惠20/实付80）。
    #   5. 微信支付正常完成。
    #   6. 支付成功后跳转结果页，三端数据同步

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-xcx-002", title="验证用户通过微信\"扫一扫\"扫商家码进入小程序支付", priority="P0")
    def test_cyzkxtzl_xcx_002(self, page):
        """[cyzkxtzl-xcx-002] 验证用户通过微信\"扫一扫\"扫商家码进入小程序支付  [P0]"""
        # ── 前置条件 ──
        #   1. 商家已生成收款二维码
    #   2. 用户在微信首页
        #
        # ── 测试步骤 ──
        #   1. 用户在微信首页点击右上角"+"→"扫一扫"。
    #   2. 扫描商家收款二维码。
    #   3. 微信识别为小程序码后自动打开快乐羊毛小程序。
    #   4. 小程序直接跳转到支付确认页。
    #   5. 完成支付
        #
        # ── 预期结果 ──
        #   1. 微信扫一扫正确识别二维码。
    #   2. 自动打开快乐羊毛小程序（冷启动或热启动）。
    #   3. 通过scene参数直接跳转到支付页面，不经过首页。
    #   4. 支付确认页展示正确金额。
    #   5. 支付流程正常完成

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-001", title="验证商家输入消费原价后折扣金额自动计算", priority="P1")
    def test_cyzkxtzl_001(self, page):
        """[cyzkxtzl-001] 验证商家输入消费原价后折扣金额自动计算  [P1]"""
        # ── 前置条件 ──
        #   商家已登录商家端，后台设置折扣率为8折
        #
        # ── 测试步骤 ──
        #   1. 进入商家端收款页面。
    #   2. 输入消费原价100元。
    #   3. 查看系统计算结果
        #
        # ── 预期结果 ──
        #   1. 收款页面正常展示。
    #   2. 输入框显示100元。
    #   3. 系统自动计算并展示：原价100元，折扣8折，优惠20元，实付金额80元

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-002", title="验证折扣计算的金额精度（含小数）", priority="P1")
    def test_cyzkxtzl_002(self, page):
        """[cyzkxtzl-002] 验证折扣计算的金额精度（含小数）  [P1]"""
        # ── 前置条件 ──
        #   商家端折扣率设置为8.5折
        #
        # ── 测试步骤 ──
        #   1. 进入商家端收款页面。
    #   2. 输入消费原价99元。
    #   3. 查看系统计算结果
        #
        # ── 预期结果 ──
        #   1. 收款页面正常展示。
    #   2. 输入框显示99元。
    #   3. 系统计算实付金额为84.15元（99 x 0.85 = 84.15），精确到分

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-004", title="验证输入原价为0时的处理 [反向]", priority="P1")
    def test_cyzkxtzl_004(self, page):
        """[cyzkxtzl-004] 验证输入原价为0时的处理 [反向]  [P1]"""
        # ── 前置条件 ──
        #   商家已登录商家端
        #
        # ── 测试步骤 ──
        #   1. 进入商家端收款页面。
    #   2. 输入消费原价0元。
    #   3. 查看系统提示
        #
        # ── 预期结果 ──
        #   1. 收款页面正常展示。
    #   2. 输入框显示0。
    #   3. 系统提示"消费金额必须大于0"，不进行折扣计算

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-009", title="验证支付二维码正常生成", priority="P1")
    def test_cyzkxtzl_009(self, page):
        """[cyzkxtzl-009] 验证支付二维码正常生成  [P1]"""
        # ── 前置条件 ──
        #   商家已输入消费原价100元，折扣率8折，系统计算实付80元
        #
        # ── 测试步骤 ──
        #   1. 商家点击"生成收款码"按钮。
    #   2. 查看二维码展示
        #
        # ── 预期结果 ──
        #   1. 系统生成支付二维码。
    #   2. 二维码清晰可扫，页面展示原价100元、优惠20元、实付80元

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-010", title="验证用户扫码后支付确认页信息展示", priority="P1")
    def test_cyzkxtzl_010(self, page):
        """[cyzkxtzl-010] 验证用户扫码后支付确认页信息展示  [P1]"""
        # ── 前置条件 ──
        #   商家已生成收款二维码，原价100元，优惠20元，实付80元
        #
        # ── 测试步骤 ──
        #   1. 用户使用微信扫描二维码。
    #   2. 查看支付确认页展示信息
        #
        # ── 预期结果 ──
        #   1. 微信识别二维码并打开支付页面。
    #   2. 支付确认页展示：原价100元、优惠20元、实付80元，信息与商家端一致

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-011", title="验证用户完成微信支付", priority="P1")
    def test_cyzkxtzl_011(self, page):
        """[cyzkxtzl-011] 验证用户完成微信支付  [P1]"""
        # ── 前置条件 ──
        #   用户已扫码进入支付确认页，实付金额80元，微信余额充足
        #
        # ── 测试步骤 ──
        #   1. 用户确认支付信息。
    #   2. 点击"确认支付"。
    #   3. 输入微信支付密码。
    #   4. 查看支付结果
        #
        # ── 预期结果 ──
        #   1. 支付信息展示实付80元。
    #   2. 弹出微信支付密码输入框。
    #   3. 密码验证通过。
    #   4. 支付成功，页面显示"支付成功"，订单状态变为"已支付"

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-012", title="验证支付成功后资金进入平台账户", priority="P1")
    def test_cyzkxtzl_012(self, page):
        """[cyzkxtzl-012] 验证支付成功后资金进入平台账户  [P1]"""
        # ── 前置条件 ──
        #   用户已完成80元支付
        #
        # ── 测试步骤 ──
        #   1. 用户完成支付。
    #   2. 查看平台后台资金流水记录
        #
        # ── 预期结果 ──
        #   1. 支付成功。
    #   2. 平台账户收到80元入账，流水记录显示对应订单号、金额80元、状态"已到账"

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-017", title="验证用户发起退款流程", priority="P1")
    def test_cyzkxtzl_017(self, page):
        """[cyzkxtzl-017] 验证用户发起退款流程  [P1]"""
        # ── 前置条件 ──
        #   用户已完成一笔餐饮折扣订单支付，实付金额80元，订单状态为"已支付"
        #
        # ── 测试步骤 ──
        #   1. 进入用户端订单详情页。
    #   2. 点击"申请退款"按钮。
    #   3. 选择退款原因"商家未提供服务"。
    #   4. 提交退款申请。
    #   5. 查看订单状态
        #
        # ── 预期结果 ──
        #   1. 订单详情页展示退款入口。
    #   2. 弹出退款申请页面。
    #   3. 退款原因选择成功。
    #   4. 提交成功，提示"退款申请已提交"。
    #   5. 订单状态变为"退款中"

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-018", title="验证商家发起退款流程", priority="P1")
    def test_cyzkxtzl_018(self, page):
        """[cyzkxtzl-018] 验证商家发起退款流程  [P1]"""
        # ── 前置条件 ──
        #   商家端有一笔已支付订单，实付金额80元
        #
        # ── 测试步骤 ──
        #   1. 进入商家端订单详情页。
    #   2. 点击"退款"按钮。
    #   3. 输入退款原因"顾客要求退款"。
    #   4. 确认退款。
    #   5. 查看订单状态
        #
        # ── 预期结果 ──
        #   1. 订单详情页展示退款按钮。
    #   2. 弹出退款确认弹窗。
    #   3. 退款原因输入成功。
    #   4. 退款操作确认成功。
    #   5. 订单状态变为"退款中"

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-019", title="验证退款资金原路退回", priority="P1")
    def test_cyzkxtzl_019(self, page):
        """[cyzkxtzl-019] 验证退款资金原路退回  [P1]"""
        # ── 前置条件 ──
        #   用户通过微信支付80元，退款申请已通过
        #
        # ── 测试步骤 ──
        #   1. 退款申请通过后等待退款处理。
    #   2. 查看用户微信账户退款到账情况。
    #   3. 查看平台后台资金流水
        #
        # ── 预期结果 ──
        #   1. 系统发起原路退回。
    #   2. 用户微信账户在1-3个工作日内收到80元退款。
    #   3. 平台后台资金流水记录该笔退款80元

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-020", title="验证退款后订单状态流转", priority="P1")
    def test_cyzkxtzl_020(self, page):
        """[cyzkxtzl-020] 验证退款后订单状态流转  [P1]"""
        # ── 前置条件 ──
        #   用户退款申请已提交，实付金额80元
        #
        # ── 测试步骤 ──
        #   1. 运营审核通过退款申请。
    #   2. 查看用户端订单状态。
    #   3. 查看商家端订单状态。
    #   4. 查看平台后台订单状态
        #
        # ── 预期结果 ──
        #   1. 审核操作成功。
    #   2. 用户端订单状态变为"已退款"。
    #   3. 商家端订单状态变为"已退款"。
    #   4. 平台后台订单状态为"已退款"，退款金额80元

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-025", title="商家输入原价-异常金额校验", priority="P1")
    def test_cyzkxtzl_025(self, page):
        """[cyzkxtzl-025] 商家输入原价-异常金额校验  [P1]"""
        # ── 前置条件 ──
        #   商家已登录商家端收银台
        #
        # ── 测试步骤 ──
        #   1. 输入金额"0"
    #   2. 输入金额"-50"
    #   3. 输入金额"abc"
    #   4. 输入金额"99999999"
    #   5. 输入金额"100.999"（3位小数）
        #
        # ── 预期结果 ──
        #   1. 输入0：提示"金额必须大于0"
    #   2. 输入负数：提示"请输入正确金额"
    #   3. 输入非数字：不允许输入/提示格式错误
    #   4. 超大金额：提示"超出限额"
    #   5. 3位小数：自动截断为2位或提示

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-xcx-003", title="验证二维码生成后的小程序前后台切换不影响有效性", priority="P1")
    def test_cyzkxtzl_xcx_003(self, page):
        """[cyzkxtzl-xcx-003] 验证二维码生成后的小程序前后台切换不影响有效性  [P1]"""
        # ── 前置条件 ──
        #   1. 商家端小程序已生成收款二维码
    #   2. 二维码有15分钟倒计时
        #
        # ── 测试步骤 ──
        #   1. 商家生成二维码后按Home键将小程序切到后台。
    #   2. 等待3分钟。
    #   3. 回到小程序前台。
    #   4. 检查二维码和倒计时状态。
    #   5. 让用户扫码支付
        #
        # ── 预期结果 ──
        #   1. 切回前台后二维码仍然显示。
    #   2. 倒计时正确更新（从服务端同步时间而非本地计时）。
    #   3. 用户扫码支付正常完成。
    #   4. 如小程序被系统回收重启，重新加载收银台页面

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-xcx-004", title="验证餐饮折扣收银台小程序数字键盘输入金额", priority="P1")
    def test_cyzkxtzl_xcx_004(self, page):
        """[cyzkxtzl-xcx-004] 验证餐饮折扣收银台小程序数字键盘输入金额  [P1]"""
        # ── 前置条件 ──
        #   1. 商家端小程序已打开收银台页面
        #
        # ── 测试步骤 ──
        #   1. 点击金额输入框。
    #   2. 观察弹出的键盘类型。
    #   3. 输入金额"99.50"。
    #   4. 点击输入框外收起键盘。
    #   5. 检查折扣自动计算
        #
        # ── 预期结果 ──
        #   1. 弹出小程序数字键盘（type="digit"带小数点）。
    #   2. 键盘不遮挡金额和折扣展示区域（页面自动上推）。
    #   3. 输入99.50后实时计算折扣（如8折=79.60）。
    #   4. 收起键盘后金额保持。
    #   5. 计算结果正确显示

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-003", title="验证折扣计算结果四舍五入处理", priority="P2")
    def test_cyzkxtzl_003(self, page):
        """[cyzkxtzl-003] 验证折扣计算结果四舍五入处理  [P2]"""
        # ── 前置条件 ──
        #   商家端折扣率设置为8.5折
        #
        # ── 测试步骤 ──
        #   1. 输入消费原价33元。
    #   2. 查看系统计算结果
        #
        # ── 预期结果 ──
        #   1. 输入框显示33元。
    #   2. 系统计算实付金额为28.05元（33 x 0.85 = 28.05），金额精确到分，无多余小数位

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-005", title="验证输入负数金额的处理 [反向]", priority="P2")
    def test_cyzkxtzl_005(self, page):
        """[cyzkxtzl-005] 验证输入负数金额的处理 [反向]  [P2]"""
        # ── 前置条件 ──
        #   商家已登录商家端
        #
        # ── 测试步骤 ──
        #   1. 进入商家端收款页面。
    #   2. 尝试输入-50元。
    #   3. 查看系统处理
        #
        # ── 预期结果 ──
        #   1. 收款页面正常展示。
    #   2. 输入框不允许输入负号。
    #   3. 无法输入负数金额

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-006", title="验证输入超大金额的处理 [反向]", priority="P2")
    def test_cyzkxtzl_006(self, page):
        """[cyzkxtzl-006] 验证输入超大金额的处理 [反向]  [P2]"""
        # ── 前置条件 ──
        #   商家已登录商家端，折扣率为8折
        #
        # ── 测试步骤 ──
        #   1. 输入消费原价999999元。
    #   2. 查看系统提示
        #
        # ── 预期结果 ──
        #   1. 输入框显示999999。
    #   2. 系统提示"单笔消费金额不能超过50000元"或按业务规则限制提示

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-007", title="验证输入非数字字符的处理 [反向]", priority="P2")
    def test_cyzkxtzl_007(self, page):
        """[cyzkxtzl-007] 验证输入非数字字符的处理 [反向]  [P2]"""
        # ── 前置条件 ──
        #   商家已登录商家端
        #
        # ── 测试步骤 ──
        #   1. 进入商家端收款页面。
    #   2. 尝试输入"abc"或中文"一百"。
    #   3. 查看系统处理
        #
        # ── 预期结果 ──
        #   1. 收款页面正常展示。
    #   2. 输入框只允许输入数字和小数点。
    #   3. 非数字字符无法输入

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-013", title="验证支付成功后订单状态流转", priority="P2")
    def test_cyzkxtzl_013(self, page):
        """[cyzkxtzl-013] 验证支付成功后订单状态流转  [P2]"""
        # ── 前置条件 ──
        #   用户已完成支付
        #
        # ── 测试步骤 ──
        #   1. 查看用户端订单状态。
    #   2. 查看商家端订单状态。
    #   3. 查看平台后台订单状态
        #
        # ── 预期结果 ──
        #   1. 用户端显示订单状态为"已支付"。
    #   2. 商家端显示该订单状态为"已支付"，并有新订单提醒。
    #   3. 平台后台该订单状态为"已支付"

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-014", title="验证二维码超时失效处理 [反向]", priority="P2")
    def test_cyzkxtzl_014(self, page):
        """[cyzkxtzl-014] 验证二维码超时失效处理 [反向]  [P2]"""
        # ── 前置条件 ──
        #   商家已生成收款二维码，超过15分钟未被扫码
        #
        # ── 测试步骤 ──
        #   1. 等待二维码超时（15分钟）。
    #   2. 用户扫描该二维码
        #
        # ── 预期结果 ──
        #   1. 二维码超时失效。
    #   2. 用户扫码后提示"该收款码已失效，请联系商家重新生成"

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-015", title="验证用户取消支付的处理 [反向]", priority="P2")
    def test_cyzkxtzl_015(self, page):
        """[cyzkxtzl-015] 验证用户取消支付的处理 [反向]  [P2]"""
        # ── 前置条件 ──
        #   用户已扫码进入支付确认页
        #
        # ── 测试步骤 ──
        #   1. 用户在支付确认页点击"取消"或返回。
    #   2. 查看订单状态
        #
        # ── 预期结果 ──
        #   1. 退出支付页面，返回上一页。
    #   2. 订单状态为"待支付"，商家端可看到用户未完成支付

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-021", title="验证部分退款场景处理", priority="P2")
    def test_cyzkxtzl_021(self, page):
        """[cyzkxtzl-021] 验证部分退款场景处理  [P2]"""
        # ── 前置条件 ──
        #   用户已支付80元，商家协商后同意退款40元
        #
        # ── 测试步骤 ──
        #   1. 运营在后台发起部分退款，输入退款金额40元。
    #   2. 确认退款。
    #   3. 查看订单状态及退款记录
        #
        # ── 预期结果 ──
        #   1. 退款金额输入40元。
    #   2. 退款操作成功。
    #   3. 订单状态为"部分退款"，退款记录显示退款40元，用户微信收到40元退款

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-022", title="验证退款金额超过实付金额的拦截 [反向]", priority="P2")
    def test_cyzkxtzl_022(self, page):
        """[cyzkxtzl-022] 验证退款金额超过实付金额的拦截 [反向]  [P2]"""
        # ── 前置条件 ──
        #   订单实付金额为80元
        #
        # ── 测试步骤 ──
        #   1. 运营在后台发起退款，输入退款金额100元。
    #   2. 查看系统提示
        #
        # ── 预期结果 ──
        #   1. 输入退款金额100元。
    #   2. 系统提示"退款金额不能超过实付金额80元"，无法提交退款

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-023", title="验证重复退款的拦截 [反向]", priority="P2")
    def test_cyzkxtzl_023(self, page):
        """[cyzkxtzl-023] 验证重复退款的拦截 [反向]  [P2]"""
        # ── 前置条件 ──
        #   一笔订单已完成全额退款80元，订单状态为"已退款"
        #
        # ── 测试步骤 ──
        #   1. 进入该订单详情页。
    #   2. 查看退款操作入口
        #
        # ── 预期结果 ──
        #   1. 订单详情页展示"已退款"状态。
    #   2. 退款按钮置灰或隐藏，无法再次发起退款

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-008", title="验证不同折扣率下的计算准确性", priority="P3")
    def test_cyzkxtzl_008(self, page):
        """[cyzkxtzl-008] 验证不同折扣率下的计算准确性  [P3]"""
        # ── 前置条件 ──
        #   后台可配置折扣率
        #
        # ── 测试步骤 ──
        #   1. 后台设置折扣率为7折，商家输入原价200元。
    #   2. 后台设置折扣率为9.5折，商家输入原价200元
        #
        # ── 预期结果 ──
        #   1. 实付金额计算为140元（200 x 0.7）。
    #   2. 实付金额计算为190元（200 x 0.95）

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-016", title="验证微信支付余额不足时的提示 [反向]", priority="P3")
    def test_cyzkxtzl_016(self, page):
        """[cyzkxtzl-016] 验证微信支付余额不足时的提示 [反向]  [P3]"""
        # ── 前置条件 ──
        #   用户微信零钱余额为50元，银行卡未绑定，实付金额80元
        #
        # ── 测试步骤 ──
        #   1. 用户扫码进入支付确认页。
    #   2. 点击确认支付。
    #   3. 查看支付结果
        #
        # ── 预期结果 ──
        #   1. 支付确认页展示实付80元。
    #   2. 发起支付请求。
    #   3. 微信提示余额不足，引导用户更换支付方式或绑定银行卡

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("cyzkxtzl-024", title="验证退款过程中的通知提醒", priority="P3")
    def test_cyzkxtzl_024(self, page):
        """[cyzkxtzl-024] 验证退款过程中的通知提醒  [P3]"""
        # ── 前置条件 ──
        #   用户发起退款申请，退款已审核通过
        #
        # ── 测试步骤 ──
        #   1. 运营审核通过退款。
    #   2. 查看用户是否收到通知
        #
        # ── 预期结果 ──
        #   1. 退款审核通过。
    #   2. 用户收到订阅消息或短信通知"您的退款申请已通过，退款金额80元将原路退回"

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")
