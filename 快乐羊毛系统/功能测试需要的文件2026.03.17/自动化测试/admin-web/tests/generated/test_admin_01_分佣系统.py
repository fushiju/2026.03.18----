# -*- coding: utf-8 -*-
"""
admin_01_分佣系统 · 自动化测试
共 71 条用例
来源模块: 分佣场景补充, 分佣系统, 分佣系统 (V3.0补充), 分佣系统 (复盘补漏), 分佣规则补充, 退款分佣冲抵 (深度验证)

运行方法:
  pytest admin-web/tests/generated\test_admin_01_分佣系统.py -v --headed     # 有界面
  pytest admin-web/tests/generated\test_admin_01_分佣系统.py -v              # 无头模式
  pytest admin-web/tests/generated\test_admin_01_分佣系统.py -k "test_fyxt"  # 只跑编号含fyxt的
"""
import pytest
import sys
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
import os
from utils.case_mapping import case


class Test分佣系统:
    """admin_01_分佣系统 (71条用例)"""

    @case("tk-fy-001", title="验证全额退款后各角色佣金冲抵正确（无分销员场景）", priority="P0")
    def test_tk_fy_001(self, page):
        """[tk-fy-001] 验证全额退款后各角色佣金冲抵正确（无分销员场景）  [P0]"""
        # ── 前置条件 ──
        #   1. 订单实付100元，成本60元，利润40元
    #   2. 平台50%(20元)，代理商50%(20元)
    #   3. 佣金状态：待结算
        #
        # ── 测试步骤 ──
        #   1. 对该订单执行全额退款。
    #   2. 查看平台佣金明细。
    #   3. 查看代理商佣金明细。
    #   4. 验证冲抵金额
        #
        # ── 预期结果 ──
        #   1. 平台佣金明细新增冲抵记录：-20.00元。
    #   2. 代理商佣金明细新增冲抵记录：-20.00元。
    #   3. 待结算余额各减少20.00元。
    #   4. 冲抵总和(-40元)=原利润(-40元)

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("tk-fy-002", title="验证全额退款后多级分销员的佣金连锁冲抵", priority="P0")
    def test_tk_fy_002(self, page):
        """[tk-fy-002] 验证全额退款后多级分销员的佣金连锁冲抵  [P0]"""
        # ── 前置条件 ──
        #   1. 利润40元，平台20元，代理商实得14元，一级4元，二级2元
    #   2. 全部处于"待结算"状态
        #
        # ── 测试步骤 ──
        #   1. 执行全额退款。
    #   2. 逐一检查各角色佣金明细冲抵记录
        #
        # ── 预期结果 ──
        #   1. 平台冲抵：-20.00元。
    #   2. 代理商冲抵：-14.00元。
    #   3. 一级分销员冲抵：-4.00元。
    #   4. 二级分销员冲抵：-2.00元。
    #   5. 合计冲抵-(20+14+4+2)=-40元=利润

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("tk-fy-003", title="验证部分退款后分佣重算的正确性", priority="P0")
    def test_tk_fy_003(self, page):
        """[tk-fy-003] 验证部分退款后分佣重算的正确性  [P0]"""
        # ── 前置条件 ──
        #   1. 订单实付100元，成本60元，利润40元
    #   2. 平台50%(20元)，代理商50%(20元)
        #
        # ── 测试步骤 ──
        #   1. 执行部分退款40元。
    #   2. 计算新利润=新实付(60)-成本(60)=0元。
    #   3. 检查各角色佣金调整。
    #   4. 验证冲抵金额
        #
        # ── 预期结果 ──
        #   1. 新利润为0元（或按比例退还成本后重算）。
    #   2. 平台佣金调整为0，冲抵记录-20元。
    #   3. 代理商佣金调整为0，冲抵记录-20元。
    #   4. 冲抵金额精确到分

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("tk-fy-004", title="验证多次部分退款后累计冲抵的正确性", priority="P0")
    def test_tk_fy_004(self, page):
        """[tk-fy-004] 验证多次部分退款后累计冲抵的正确性  [P0]"""
        # ── 前置条件 ──
        #   1. 订单实付100元，成本60元，利润40元
    #   2. 分佣已生成
        #
        # ── 测试步骤 ──
        #   1. 第一次部分退款30元，检查分佣调整。
    #   2. 第二次部分退款20元，检查分佣再次调整。
    #   3. 第三次尝试退款60元（超出剩余可退50元）
        #
        # ── 预期结果 ──
        #   1. 第一次退款后分佣按新利润重算。
    #   2. 第二次退款后分佣再次重算。
    #   3. 第三次退款被拦截："退款金额不能超过剩余可退50元"。
    #   4. 累计冲抵金额正确

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("tk-fy-005", title="验证混合状态下退款冲抵（部分已提现/部分待结算）", priority="P0")
    def test_tk_fy_005(self, page):
        """[tk-fy-005] 验证混合状态下退款冲抵（部分已提现/部分待结算）  [P0]"""
        # ── 前置条件 ──
        #   1. 利润40元，平台佣金20元(待结算)，代理商20元(已提现)
    #   2. 一级分销员4元(待结算)
        #
        # ── 测试步骤 ──
        #   1. 执行全额退款。
    #   2. 检查平台佣金（待结算→冲抵）。
    #   3. 检查代理商佣金（已提现→欠款）。
    #   4. 检查一级分销员佣金冲抵
        #
        # ── 预期结果 ──
        #   1. 平台：待结算余额-20元（直接冲抵）。
    #   2. 代理商：产生欠款记录20元（因已提现）。
    #   3. 一级分销员：待结算余额-4元（直接冲抵）。
    #   4. 各角色冲抵/欠款机制独立运作

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-bl-001", title="验证利润为0（实付=成本）时所有角色佣金均为0", priority="P0")
    def test_fyxt_bl_001(self, page):
        """[fyxt-bl-001] 验证利润为0（实付=成本）时所有角色佣金均为0  [P0]"""
        # ── 前置条件 ──
        #   1. 后台已配置平台50%/代理商50%
    #   2. 有一级分销员10%
        #
        # ── 测试步骤 ──
        #   1. 创建一笔订单：实付金额50元，投入成本50元。
    #   2. 支付完成后查看分佣明细
        #
        # ── 预期结果 ──
        #   1. 利润=50-50=0元。
    #   2. 平台佣金=0×50%=0.00元。
    #   3. 代理商佣金=0×50%=0.00元。
    #   4. 一级分销员佣金=0×10%=0.00元。
    #   5. 所有角色佣金均为0.00元，分佣记录正常生成但金额为0

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-bl-002", title="验证利润为负（投入>实付）时后端校验拦截", priority="P0")
    def test_fyxt_bl_002(self, page):
        """[fyxt-bl-002] 验证利润为负（投入>实付）时后端校验拦截  [P0]"""
        # ── 前置条件 ──
        #   1. 管理后台已登录
        #
        # ── 测试步骤 ──
        #   1. 尝试在后台创建/配置一个商品：售价30元，成本50元。
    #   2. 保存商品配置。
    #   3. 如果保存成功，用户下单该商品后查看分佣
        #
        # ── 预期结果 ──
        #   1. 后端校验拦截：提示"售价不能低于成本"或"利润不能为负"。
    #   2. 不允许保存该配置。
    #   3. 如果系统允许（异常情况），分佣计算不应产生负数佣金

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-001", title="验证设置代理商分佣比例成功", priority="P1")
    def test_fyxt_001(self, page):
        """[fyxt-001] 验证设置代理商分佣比例成功  [P1]"""
        # ── 前置条件 ──
        #   运营登录后台管理系统
        #
        # ── 测试步骤 ──
        #   1. 进入"分佣配置"页面。
    #   2. 选择业务线"餐饮折扣"。
    #   3. 设置代理商分佣比例为5%。
    #   4. 点击"保存"
        #
        # ── 预期结果 ──
        #   1. 分佣配置页面正常展示。
    #   2. 业务线"餐饮折扣"被选中。
    #   3. 比例输入成功。
    #   4. 提示"保存成功"，列表显示餐饮折扣代理商分佣5%

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-002", title="验证设置分销员分佣比例等级", priority="P1")
    def test_fyxt_002(self, page):
        """[fyxt-002] 验证设置分销员分佣比例等级  [P1]"""
        # ── 前置条件 ──
        #   运营登录后台
        #
        # ── 测试步骤 ──
        #   1. 进入分佣配置。
    #   2. 选择业务线"会员充值"。
    #   3. 设置一级分销员比例3%，二级分销员比例1%。
    #   4. 点击"保存"
        #
        # ── 预期结果 ──
        #   1. 页面正常展示。
    #   2. 业务线选择成功。
    #   3. 两级比例输入成功。
    #   4. 提示"保存成功"，展示会员充值分销员一级3%、二级1%

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-003", title="验证餐饮折扣扫码支付分佣计算准确性", priority="P1")
    def test_fyxt_003(self, page):
        """[fyxt-003] 验证餐饮折扣扫码支付分佣计算准确性  [P1]"""
        # ── 前置条件 ──
        #   餐饮折扣代理商分佣5%，分销员一级3%。一笔扫码支付订单金额100元
        #
        # ── 测试步骤 ──
        #   1. 用户完成一笔100元餐饮折扣扫码支付。
    #   2. 查看代理商佣金。
    #   3. 查看分销员佣金
        #
        # ── 预期结果 ──
        #   1. 订单状态为"已完成"。
    #   2. 代理商佣金显示5.00元。
    #   3. 一级分销员佣金显示3.00元

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-009", title="验证导入正确格式Excel自动计算分佣成功", priority="P1")
    def test_fyxt_009(self, page):
        """[fyxt-009] 验证导入正确格式Excel自动计算分佣成功  [P1]"""
        # ── 前置条件 ──
        #   运营登录后台，准备Excel文件包含3条记录：用户ID(U001/U002/U003)、订单金额(100/200/150)、佣金(5/10/7.5)
        #
        # ── 测试步骤 ──
        #   1. 进入"佣金录入"页面。
    #   2. 点击"导入Excel"。
    #   3. 选择准备好的Excel文件上传。
    #   4. 确认导入。
    #   5. 查看佣金分发记录
        #
        # ── 预期结果 ──
        #   1. 佣金录入页面正常展示。
    #   2. 文件选择弹窗出现。
    #   3. 文件上传成功，预览显示3条记录。
    #   4. 提示"导入成功，共3条记录"。
    #   5. U001佣金5元、U002佣金10元、U003佣金7.5元正确入账

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-017", title="验证查看分销员待结算金额", priority="P1")
    def test_fyxt_017(self, page):
        """[fyxt-017] 验证查看分销员待结算金额  [P1]"""
        # ── 前置条件 ──
        #   分销员"张三"有3笔待结算佣金，分别为5元、10元、7.5元
        #
        # ── 测试步骤 ──
        #   1. 运营登录后台，进入"结算管理"页面。
    #   2. 搜索分销员"张三"。
    #   3. 查看待结算明细
        #
        # ── 预期结果 ──
        #   1. 结算管理页面正常展示。
    #   2. 搜索结果显示"张三"。
    #   3. 待结算总金额22.50元，明细列表显示3笔记录

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-018", title="验证审核通过提现申请", priority="P1")
    def test_fyxt_018(self, page):
        """[fyxt-018] 验证审核通过提现申请  [P1]"""
        # ── 前置条件 ──
        #   分销员"张三"发起提现申请20元，当前可提现余额22.50元
        #
        # ── 测试步骤 ──
        #   1. 进入提现申请列表。
    #   2. 找到"张三"的提现申请。
    #   3. 点击"审核通过"。
    #   4. 查看"张三"余额变化
        #
        # ── 预期结果 ──
        #   1. 提现列表正常展示。
    #   2. 显示申请金额20元，状态"待审核"。
    #   3. 提示"审核通过"，状态变为"已打款"。
    #   4. 可提现余额变为2.50元

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-024", title="CPS类-Excel手动录入佣金", priority="P1")
    def test_fyxt_024(self, page):
        """[fyxt-024] CPS类-Excel手动录入佣金  [P1]"""
        # ── 前置条件 ──
        #   运营准备佣金Excel（3条数据）
        #
        # ── 测试步骤 ──
        #   1. 下载导入模板
    #   2. 填写：用户A-100元-5元佣金
    #   3. 填写：用户B-200元-10元佣金
    #   4. 填写：用户C-50元-2.5元佣金
    #   5. 上传导入
    #   6. 查看处理结果
        #
        # ── 预期结果 ──
        #   1. 模板字段：用户ID/订单金额/佣金金额
    #   2. 导入成功，处理3条
    #   3. 用户A佣金记录+5元
    #   4. 用户B佣金记录+10元
    #   5. 用户C佣金记录+2.5元
    #   6. 系统根据分佣比例自动分发给各角色

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-025", title="只分利润部分验证", priority="P1")
    def test_fyxt_025(self, page):
        """[fyxt-025] 只分利润部分验证  [P1]"""
        # ── 前置条件 ──
        #   一笔订单总金额100元，成本/投入60元
        #
        # ── 测试步骤 ──
        #   1. 查看分佣计算明细
    #   2. 核对分佣基数
        #
        # ── 预期结果 ──
        #   1. 分佣基数=利润部分=100-60=40元
    #   2. 各角色基于40元计算佣金
    #   3. 成本60元不参与分佣

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-026", title="联盟订单同步佣金", priority="P1")
    def test_fyxt_026(self, page):
        """[fyxt-026] 联盟订单同步佣金  [P1]"""
        # ── 前置条件 ──
        #   用户通过推广链接在淘宝消费
        #
        # ── 测试步骤 ──
        #   1. 等待联盟订单同步
    #   2. 查看后台佣金记录
    #   3. 核对推广者ID匹配
        #
        # ── 预期结果 ──
        #   1. 联盟订单出现在后台
    #   2. 推广者ID正确匹配到本平台用户
    #   3. 佣金状态为"待结算"
    #   4. 确认收货后变为"可结算"

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fygz-001", title="验证分佣仅计算利润部分不含投入成本", priority="P1")
    def test_fygz_001(self, page):
        """[fygz-001] 验证分佣仅计算利润部分不含投入成本  [P1]"""
        # ── 前置条件 ──
        #   一笔餐饮订单消费100元，折扣后实付85元，其中投入成本（原价/进货价）为50元，利润为35元。代理商分佣比例10%
        #
        # ── 测试步骤 ──
        #   1. 用户完成支付85元。
    #   2. 查看代理商佣金计算
        #
        # ── 预期结果 ──
        #   1. 支付成功，订单完成。
    #   2. 代理商佣金为3.50元（利润35元×10%），不是8.50元（实付85元×10%）

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fygz-002", title="验证分销员佣金也基于利润部分计算", priority="P1")
    def test_fygz_002(self, page):
        """[fygz-002] 验证分销员佣金也基于利润部分计算  [P1]"""
        # ── 前置条件 ──
        #   同上订单，一级分销员分佣比例5%
        #
        # ── 测试步骤 ──
        #   1. 订单完成后查看分销员佣金
        #
        # ── 预期结果 ──
        #   1. 一级分销员佣金为1.75元（利润35元×5%），非基于实付金额计算

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fygz-003", title="验证平台、代理商、分销员三方分佣总和不超过利润", priority="P1")
    def test_fygz_003(self, page):
        """[fygz-003] 验证平台、代理商、分销员三方分佣总和不超过利润  [P1]"""
        # ── 前置条件 ──
        #   利润35元，平台分佣15%（5.25元）、代理商10%（3.50元）、分销员5%（1.75元），总分佣30%
        #
        # ── 测试步骤 ──
        #   1. 订单完成后查看各方佣金明细。
    #   2. 计算分佣总和
        #
        # ── 预期结果 ──
        #   1. 平台5.25元、代理商3.50元、分销员1.75元。
    #   2. 分佣总和10.50元，占利润30%，未超过利润35元

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-001", title="验证分销员推广用户下单时代理商和分销员同时分佣", priority="P1")
    def test_fycj_001(self, page):
        """[fycj-001] 验证分销员推广用户下单时代理商和分销员同时分佣  [P1]"""
        # ── 前置条件 ──
        #   代理商比例30%，一级分销员比例10%（从代理商份额扣除），用户通过分销员推广链接下单，订单利润100元
        #
        # ── 测试步骤 ──
        #   1. 分销员分享推广链接给用户
    #   2. 用户通过链接下单并完成支付
    #   3. 查看代理商佣金
    #   4. 查看分销员佣金
        #
        # ── 预期结果 ──
        #   1. 分销员佣金=100×10%=10元
    #   2. 代理商佣金=100×(30%-10%)=20元
    #   3. 分销员佣金从代理商份额扣除
    #   4. 两者佣金之和不超过代理商原始比例30%

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-002", title="验证无分销员时代理商获得完整佣金", priority="P1")
    def test_fycj_002(self, page):
        """[fycj-002] 验证无分销员时代理商获得完整佣金  [P1]"""
        # ── 前置条件 ──
        #   代理商比例30%，订单无关联分销员，利润100元
        #
        # ── 测试步骤 ──
        #   1. 用户直接下单（非推广链接）
    #   2. 订单完成
    #   3. 查看代理商佣金
        #
        # ── 预期结果 ──
        #   1. 代理商佣金=100×30%=30元
    #   2. 无分销员分佣记录
    #   3. 代理商获得完整份额

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-004", title="验证极小金额订单分佣计算（利润0.01元）", priority="P1")
    def test_fycj_004(self, page):
        """[fycj-004] 验证极小金额订单分佣计算（利润0.01元）  [P1]"""
        # ── 前置条件 ──
        #   订单利润0.01元，代理商比例30%
        #
        # ── 测试步骤 ──
        #   1. 完成一笔利润仅0.01元的订单
    #   2. 查看分佣结果
        #
        # ── 预期结果 ──
        #   1. 代理商佣金=0.01×30%=0.003→四舍五入到0.00元
    #   2. 系统不产生负数或异常
    #   3. 分佣记录正常展示

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-005", title="验证分佣计算精度（无限小数场景）", priority="P1")
    def test_fycj_005(self, page):
        """[fycj-005] 验证分佣计算精度（无限小数场景）  [P1]"""
        # ── 前置条件 ──
        #   订单利润100元，分佣比例33.33%
        #
        # ── 测试步骤 ──
        #   1. 完成订单
    #   2. 查看佣金计算
    #   3. 验证各方金额总和
        #
        # ── 预期结果 ──
        #   1. 佣金=100×33.33%=33.33元（精确到分）
    #   2. 各角色佣金之和≤利润100元
    #   3. 无丢失或多出
    #   4. 不出现NaN或Infinity

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-008", title="验证全额退款后分佣记录撤销", priority="P1")
    def test_fycj_008(self, page):
        """[fycj-008] 验证全额退款后分佣记录撤销  [P1]"""
        # ── 前置条件 ──
        #   一笔餐饮订单已完成，利润100元，代理商已获佣金30元（30%），分销员已获10元（10%）
        #
        # ── 测试步骤 ──
        #   1. 运营发起全额退款
    #   2. 退款成功
    #   3. 查看代理商佣金记录
    #   4. 查看分销员佣金记录
        #
        # ── 预期结果 ──
        #   1. 退款原路退回
    #   2. 代理商佣金记录增加"-30元"冲抵记录
    #   3. 分销员佣金记录增加"-10元"冲抵记录
    #   4. 可提现余额相应减少

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-009", title="验证部分退款后分佣按实际金额重新计算", priority="P1")
    def test_fycj_009(self, page):
        """[fycj-009] 验证部分退款后分佣按实际金额重新计算  [P1]"""
        # ── 前置条件 ──
        #   订单实付200元，利润100元，代理商30%=30元。部分退款100元（退一半）
        #
        # ── 测试步骤 ──
        #   1. 发起部分退款100元
    #   2. 退款成功
    #   3. 查看分佣调整
        #
        # ── 预期结果 ──
        #   1. 退款成功
    #   2. 利润调整为50元
    #   3. 代理商佣金调整为15元（50×30%）
    #   4. 差额15元从代理商待结算扣除

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-010", title="验证佣金已提现后退款的处理", priority="P1")
    def test_fycj_010(self, page):
        """[fycj-010] 验证佣金已提现后退款的处理  [P1]"""
        # ── 前置条件 ──
        #   代理商佣金30元已提现到账，后发生退款
        #
        # ── 测试步骤 ──
        #   1. 代理商提现30元成功
    #   2. 订单发生全额退款
    #   3. 查看代理商余额
        #
        # ── 预期结果 ──
        #   1. 提现已到账
    #   2. 退款后系统产生"-30元"欠款记录
    #   3. 代理商可提现余额变为负数或从下笔佣金中扣除
    #   4. 后台财务标记待追回

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-012", title="验证餐饮人工辅助订单完成后分佣+消息通知全链路", priority="P1")
    def test_fycj_012(self, page):
        """[fycj-012] 验证餐饮人工辅助订单完成后分佣+消息通知全链路  [P1]"""
        # ── 前置条件 ──
        #   用户通过分销员推广进入，在人工辅助模式商家下单
        #
        # ── 测试步骤 ──
        #   1. 分销员分享链接
    #   2. 用户选择人工辅助商家下单（原价100，折后85）
    #   3. 上传凭证提交
    #   4. 运营审核通过输入验证码
    #   5. 商家核销
    #   6. 检查分佣
    #   7. 检查消息通知
        #
        # ── 预期结果 ──
        #   1. 订单关联分销员ID
    #   2. 订单完成后分佣基于利润计算
    #   3. 分销员获得佣金
    #   4. 代理商获得佣金
    #   5. 用户收到核销验证码通知
    #   6. 分销员收到佣金到账通知

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-013", title="验证选品仓库上架商品后前端购买到分佣全链路", priority="P1")
    def test_fycj_013(self, page):
        """[fycj-013] 验证选品仓库上架商品后前端购买到分佣全链路  [P1]"""
        # ── 前置条件 ──
        #   运营通过选品仓库同步并上架一个通用券商品
        #
        # ── 测试步骤 ──
        #   1. 选品仓库一键同步
    #   2. 运营上架商品
    #   3. 前端用户查看并购买
    #   4. 支付成功卡密下发
    #   5. 查看分佣记录
        #
        # ── 预期结果 ──
        #   1. 同步成功并上架
    #   2. 前端展示新商品
    #   3. 用户购买支付成功
    #   4. 卡密正确下发
    #   5. API佣金→平台扣除→分销员获佣
    #   6. 全链路无断裂

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-015", title="验证待结算佣金到可提现的转化", priority="P1")
    def test_fycj_015(self, page):
        """[fycj-015] 验证待结算佣金到可提现的转化  [P1]"""
        # ── 前置条件 ──
        #   分销员有3笔待结算佣金，订单均已确认完成超过结算周期
        #
        # ── 测试步骤 ──
        #   1. 等待结算周期到达
    #   2. 查看分销员佣金状态变化
        #
        # ── 预期结果 ──
        #   1. 待结算佣金自动转为可提现
    #   2. 可提现金额=3笔佣金之和
    #   3. 待结算减少对应金额

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-016", title="验证提现金额不能超过可提现余额", priority="P1")
    def test_fycj_016(self, page):
        """[fycj-016] 验证提现金额不能超过可提现余额  [P1]"""
        # ── 前置条件 ──
        #   分销员可提现余额50元
        #
        # ── 测试步骤 ──
        #   1. 输入提现金额60元
    #   2. 提交提现申请
        #
        # ── 预期结果 ──
        #   1. 提示"提现金额不能超过可提现余额50元"
    #   2. 无法提交

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-001", title="验证平台+代理商比例合计100%,利润全部分完", priority="P1")
    def test_fybc_001(self, page):
        """[fybc-001] 验证平台+代理商比例合计100%,利润全部分完  [P1]"""
        # ── 前置条件 ──
        #   平台比例50%,代理商比例50%,合计100%。订单利润100元
        #
        # ── 测试步骤 ──
        #   1. 完成一笔利润100元的订单
    #   2. 查看各方分佣明细
    #   3. 核对剩余50元的归属
        #
        # ── 预期结果 ──
        #   1. 平台佣金=100x50%=50元
    #   2. 代理商佣金=100x50%=50元
    #   3. 合计=50+50=100元=利润(全部分完,无剩余)
    #   4. 分佣明细应清晰展示两方金额

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-002", title="验证无代理商绑定的订单分佣处理", priority="P1")
    def test_fybc_002(self, page):
        """[fybc-002] 验证无代理商绑定的订单分佣处理  [P1]"""
        # ── 前置条件 ──
        #   订单未关联任何代理商,平台比例8%
        #
        # ── 测试步骤 ──
        #   1. 创建一笔未绑定代理商的订单
    #   2. 订单完成后查看分佣
    #   3. 或验证下单时是否强制要求代理商
        #
        # ── 预期结果 ──
        #   1. 如果必须有代理商:下单时应提示
    #   2. 如果允许无代理商:仅平台获得佣金
    #   3. 代理商佣金为0,分销员佣金为0

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-003", title="[反向] 验证分销员比例之和超过代理商比例时被拦截", priority="P1")
    def test_fybc_003(self, page):
        """[fybc-003] [反向] 验证分销员比例之和超过代理商比例时被拦截  [P1]"""
        # ── 前置条件 ──
        #   代理商比例42%,设置一级分销员30%,二级分销员20%(合计50%)
        #
        # ── 测试步骤 ──
        #   1. 进入分佣配置
    #   2. 设置代理商42%
    #   3. 设置一级分销员30%
    #   4. 设置二级分销员20%
    #   5. 点击保存
        #
        # ── 预期结果 ──
        #   1. 提示"分销员分佣比例之和(50%)不能超过代理商比例(42%)"
    #   2. 保存失败

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-004", title="验证后台原价(投入成本)字段正确影响分佣计算", priority="P1")
    def test_fybc_004(self, page):
        """[fybc-004] 验证后台原价(投入成本)字段正确影响分佣计算  [P1]"""
        # ── 前置条件 ──
        #   后台设置商品原价(成本)60元,用户实付100元,代理商比例10%
        #
        # ── 测试步骤 ──
        #   1. 后台查看商品原价字段
    #   2. 用户下单支付100元
    #   3. 查看分佣计算明细
        #
        # ── 预期结果 ──
        #   1. 利润=100-60=40元(不是100元)
    #   2. 代理商佣金=40x10%=4元(不是10元)
    #   3. 分佣明细展示:订单金额100元、原价/成本60元、利润40元

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-005", title="验证分佣明细中无\"技师\"相关术语", priority="P1")
    def test_fybc_005(self, page):
        """[fybc-005] 验证分佣明细中无\"技师\"相关术语  [P1]"""
        # ── 前置条件 ──
        #   后台商品原价/成本已配置
        #
        # ── 测试步骤 ──
        #   1. 查看分佣计算明细页面
    #   2. 检查字段名称
        #
        # ── 预期结果 ──
        #   1. 页面显示"投入成本"或"原价"
    #   2. 不应出现"技师费用"等今夜到家旧系统术语
    #   3. 计算逻辑:利润=实付-原价

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-006", title="验证佣金已提现后订单退款的欠款追溯", priority="P1")
    def test_fybc_006(self, page):
        """[fybc-006] 验证佣金已提现后订单退款的欠款追溯  [P1]"""
        # ── 前置条件 ──
        #   代理商佣金30元已提现,随后订单全额退款
        #
        # ── 测试步骤 ──
        #   1. 代理商提现30元成功
    #   2. 运营对该订单发起全额退款
    #   3. 查看代理商佣金记录
    #   4. 代理商产生新佣金时查看扣除
        #
        # ── 预期结果 ──
        #   1. 提现30元到账
    #   2. 退款成功
    #   3. 佣金记录增加"-30元"欠款记录
    #   4. 下笔新佣金自动扣除欠款30元

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-007", title="验证部分退款后各方分佣按新利润重新计算", priority="P1")
    def test_fybc_007(self, page):
        """[fybc-007] 验证部分退款后各方分佣按新利润重新计算  [P1]"""
        # ── 前置条件 ──
        #   订单实付200元,原价100元,利润100元。代理商10%=10元。部分退款100元
        #
        # ── 测试步骤 ──
        #   1. 发起部分退款100元
    #   2. 退款成功
    #   3. 查看分佣调整明细
        #
        # ── 预期结果 ──
        #   1. 退款后需确认利润重算规则
    #   2. 各方佣金按新利润调整
    #   3. 差额从待结算扣除

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-011", title="验证结算周期:待结算到可提现的时间转化", priority="P1")
    def test_fybc_011(self, page):
        """[fybc-011] 验证结算周期:待结算到可提现的时间转化  [P1]"""
        # ── 前置条件 ──
        #   分销员有1笔佣金处于"待结算"状态
        #
        # ── 测试步骤 ──
        #   1. 记录订单完成时间
    #   2. 等待结算周期到达
    #   3. 查看佣金状态变化
        #
        # ── 预期结果 ──
        #   1. 结算周期到达前:状态为"待结算",不可提现
    #   2. 结算周期到达后:状态自动变为"可提现"

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("tk-fy-006", title="验证修改分佣比例后已完成订单的佣金不变", priority="P1")
    def test_tk_fy_006(self, page):
        """[tk-fy-006] 验证修改分佣比例后已完成订单的佣金不变  [P1]"""
        # ── 前置条件 ──
        #   1. 订单A在分佣比例50%/50%时完成，佣金已生成
    #   2. 管理后台准备修改分佣比例
        #
        # ── 测试步骤 ──
        #   1. 查看订单A的分佣明细（平台20/代理商20）。
    #   2. 将分佣比例修改为30%/70%。
    #   3. 再次查看订单A的分佣明细。
    #   4. 创建新订单B，查看新订单的分佣
        #
        # ── 预期结果 ──
        #   1. 订单A分佣不变（仍为20/20）。
    #   2. 新订单B按新比例计算（12/28）。
    #   3. 历史数据不被篡改

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-004", title="验证按业务线区分不同分佣规则", priority="P2")
    def test_fyxt_004(self, page):
        """[fyxt-004] 验证按业务线区分不同分佣规则  [P2]"""
        # ── 前置条件 ──
        #   餐饮折扣代理商5%，外卖券代理商2%
        #
        # ── 测试步骤 ──
        #   1. 查看分佣配置列表。
    #   2. 分别查看餐饮折扣和外卖券的配置
        #
        # ── 预期结果 ──
        #   1. 列表按业务线分类展示。
    #   2. 餐饮折扣显示代理商5%，外卖券显示代理商2%，互不影响

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-005", title="验证直充/券类分佣基于API佣金比例正确计算", priority="P2")
    def test_fyxt_005(self, page):
        """[fyxt-005] 验证直充/券类分佣基于API佣金比例正确计算  [P2]"""
        # ── 前置条件 ──
        #   API返回佣金比例10%，平台扣除4%，分销员比例设为6%。订单金额50元
        #
        # ── 测试步骤 ──
        #   1. 用户完成一笔50元话费充值。
    #   2. 查看系统计算的分佣明细
        #
        # ── 预期结果 ──
        #   1. 订单完成。
    #   2. API佣金5元，平台扣除2元，分销员佣金3元（50×6%=3）

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-010", title="验证Excel预览功能和确认流程", priority="P2")
    def test_fyxt_010(self, page):
        """[fyxt-010] 验证Excel预览功能和确认流程  [P2]"""
        # ── 前置条件 ──
        #   运营上传Excel文件
        #
        # ── 测试步骤 ──
        #   1. 上传Excel后查看预览页面。
    #   2. 检查数据条数和字段映射。
    #   3. 点击"取消导入"
        #
        # ── 预期结果 ──
        #   1. 预览展示表格数据，包含用户ID、订单金额、佣金列。
    #   2. 显示"共3条记录"，字段映射正确。
    #   3. 导入取消，数据不入库

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-019", title="验证驳回提现申请", priority="P2")
    def test_fyxt_019(self, page):
        """[fyxt-019] 验证驳回提现申请  [P2]"""
        # ── 前置条件 ──
        #   分销员"李四"发起提现申请50元
        #
        # ── 测试步骤 ──
        #   1. 找到"李四"的提现申请。
    #   2. 点击"驳回"。
    #   3. 填写驳回原因"账户信息不完整，请完善后重试"。
    #   4. 确认驳回
        #
        # ── 预期结果 ──
        #   1. 显示申请详情。
    #   2. 驳回弹窗出现。
    #   3. 原因填写成功。
    #   4. 状态变为"已驳回"，提现金额退回余额

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-020", title="验证结算记录按时间筛选", priority="P2")
    def test_fyxt_020(self, page):
        """[fyxt-020] 验证结算记录按时间筛选  [P2]"""
        # ── 前置条件 ──
        #   有2026年2月和3月的多条结算记录
        #
        # ── 测试步骤 ──
        #   1. 进入结算管理页面。
    #   2. 选择时间范围2026-03-01到2026-03-13。
    #   3. 点击"查询"
        #
        # ── 预期结果 ──
        #   1. 页面正常加载。
    #   2. 时间选择成功。
    #   3. 仅显示3月1日至3月13日的结算记录

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fygz-004", title="验证投入成本（原价/进货价）在分佣明细中正确标识", priority="P2")
    def test_fygz_004(self, page):
        """[fygz-004] 验证投入成本（原价/进货价）在分佣明细中正确标识  [P2]"""
        # ── 前置条件 ──
        #   运营登录后台查看订单分佣明细
        #
        # ── 测试步骤 ──
        #   1. 进入订单详情页。
    #   2. 查看分佣计算明细
        #
        # ── 预期结果 ──
        #   1. 订单详情正常展示。
    #   2. 分佣明细显示：订单金额85元、投入部分（投入成本）50元、利润35元、各方佣金基于35元计算

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fygz-005", title="验证不同投入成本的订单分佣计算准确", priority="P2")
    def test_fygz_005(self, page):
        """[fygz-005] 验证不同投入成本的订单分佣计算准确  [P2]"""
        # ── 前置条件 ──
        #   订单A：实付200元、投入120元、利润80元。订单B：实付150元、投入60元、利润90元。代理商比例10%
        #
        # ── 测试步骤 ──
        #   1. 两笔订单完成后查看代理商佣金
        #
        # ── 预期结果 ──
        #   1. 订单A代理商佣金8.00元（80×10%），订单B代理商佣金9.00元（90×10%）

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-003", title="验证二级分销员同时存在时三方分佣", priority="P2")
    def test_fycj_003(self, page):
        """[fycj-003] 验证二级分销员同时存在时三方分佣  [P2]"""
        # ── 前置条件 ──
        #   代理商30%，一级分销员10%，二级分销员5%，利润200元
        #
        # ── 测试步骤 ──
        #   1. 二级分销员推广用户下单
    #   2. 订单完成
    #   3. 分别查看三方佣金
        #
        # ── 预期结果 ──
        #   1. 二级分销员=200×5%=10元
    #   2. 一级分销员=200×10%=20元
    #   3. 代理商=200×(30%-10%-5%)=30元
    #   4. 三方总和=60元=200×30%

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-006", title="验证大额订单分佣计算正确", priority="P2")
    def test_fycj_006(self, page):
        """[fycj-006] 验证大额订单分佣计算正确  [P2]"""
        # ── 前置条件 ──
        #   订单利润50000元，代理商30%，分销员10%
        #
        # ── 测试步骤 ──
        #   1. 完成大额订单
    #   2. 查看各方佣金
        #
        # ── 预期结果 ──
        #   1. 代理商=50000×20%=10000元
    #   2. 分销员=50000×10%=5000元
    #   3. 计算正确无溢出

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-007", title="验证分佣比例为0%时不产生佣金", priority="P2")
    def test_fycj_007(self, page):
        """[fycj-007] 验证分佣比例为0%时不产生佣金  [P2]"""
        # ── 前置条件 ──
        #   代理商比例设为0%
        #
        # ── 测试步骤 ──
        #   1. 设置代理商比例为0%
    #   2. 完成订单
    #   3. 查看代理商佣金
        #
        # ── 预期结果 ──
        #   1. 代理商佣金=0.00元
    #   2. 分佣记录显示0
    #   3. 无异常报错

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-011", title="验证退款后财务对账报表准确", priority="P2")
    def test_fycj_011(self, page):
        """[fycj-011] 验证退款后财务对账报表准确  [P2]"""
        # ── 前置条件 ──
        #   本月有10笔订单，其中2笔已退款
        #
        # ── 测试步骤 ──
        #   1. 导出本月财务对账报表
    #   2. 核对退款订单处理
        #
        # ── 预期结果 ──
        #   1. 报表正确标记退款订单
    #   2. 退款金额从总流水中扣除
    #   3. 佣金结算金额=实际未退款订单佣金总和
    #   4. 报表数据与系统一致

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-014", title="验证同一订单在三端数据一致性", priority="P2")
    def test_fycj_014(self, page):
        """[fycj-014] 验证同一订单在三端数据一致性  [P2]"""
        # ── 前置条件 ──
        #   一笔餐饮扫码订单已完成
        #
        # ── 测试步骤 ──
        #   1. 用户端查看订单详情
    #   2. 商家端查看该订单
    #   3. 管理后台查看该订单
        #
        # ── 预期结果 ──
        #   1. 三端订单金额一致
    #   2. 三端订单状态一致
    #   3. 三端时间信息一致
    #   4. 后台分佣数据只在管理端可见

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fycj-017", title="验证提现申请运营审核流程", priority="P2")
    def test_fycj_017(self, page):
        """[fycj-017] 验证提现申请运营审核流程  [P2]"""
        # ── 前置条件 ──
        #   分销员提交50元提现申请
        #
        # ── 测试步骤 ──
        #   1. 分销员提交提现
    #   2. 运营后台查看提现列表
    #   3. 审核通过
    #   4. 查看到账
        #
        # ── 预期结果 ──
        #   1. 提现申请提交成功
    #   2. 后台展示待审核提现
    #   3. 审核通过后发起打款
    #   4. 分销员微信收到50元
    #   5. 可提现余额变为0

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-008", title="验证多级分销退款时的连锁冲抵", priority="P2")
    def test_fybc_008(self, page):
        """[fybc-008] 验证多级分销退款时的连锁冲抵  [P2]"""
        # ── 前置条件 ──
        #   一级分销员佣金5元已提现,二级分销员佣金3元待结算,订单全额退款
        #
        # ── 测试步骤 ──
        #   1. 订单全额退款
    #   2. 查看一级分销员佣金记录
    #   3. 查看二级分销员佣金记录
        #
        # ── 预期结果 ──
        #   1. 一级分销员产生"-5元"欠款记录
    #   2. 二级分销员"-3元"从待结算扣除
    #   3. 代理商佣金同步冲抵

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-009", title="验证CPS跳转类Excel导入佣金后直接按比例分佣", priority="P2")
    def test_fybc_009(self, page):
        """[fybc-009] 验证CPS跳转类Excel导入佣金后直接按比例分佣  [P2]"""
        # ── 前置条件 ──
        #   运营导入Excel:用户A佣金10元,代理商42%,分销员5%
        #
        # ── 测试步骤 ──
        #   1. 运营上传Excel导入佣金
    #   2. 导入成功
    #   3. 查看各方分佣
        #
        # ── 预期结果 ──
        #   1. 导入的10元佣金直接作为可分配金额(不再扣成本)
    #   2. 代理商=10x42%=4.2元
    #   3. 分销员=10x5%=0.5元
    #   4. 平台=10x8%=0.8元

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-010", title="验证代理商自行设置分销员比例", priority="P2")
    def test_fybc_010(self, page):
        """[fybc-010] 验证代理商自行设置分销员比例  [P2]"""
        # ── 前置条件 ──
        #   代理商已登录代理商后台
        #
        # ── 测试步骤 ──
        #   1. 进入分销管理
    #   2. 设置一级分销员比例5%
    #   3. 设置二级分销员比例3%
    #   4. 保存
    #   5. 分销员推荐用户下单后查看佣金
        #
        # ── 预期结果 ──
        #   1. 比例设置成功保存
    #   2. 分销员佣金按设置比例计算
    #   3. 佣金从代理商份额中扣除

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-006", title="[反向] 验证分佣比例超过100%时保存失败", priority="P3")
    def test_fyxt_006(self, page):
        """[fyxt-006] [反向] 验证分佣比例超过100%时保存失败  [P3]"""
        # ── 前置条件 ──
        #   运营登录后台分佣配置页面
        #
        # ── 测试步骤 ──
        #   1. 选择业务线"餐饮折扣"。
    #   2. 设置代理商比例60%，一级分销员50%。
    #   3. 点击"保存"
        #
        # ── 预期结果 ──
        #   1. 业务线选中。
    #   2. 比例输入成功。
    #   3. 提示"分佣比例总和不能超过100%"，保存失败

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-007", title="[反向] 验证分佣比例输入负数被拦截", priority="P3")
    def test_fyxt_007(self, page):
        """[fyxt-007] [反向] 验证分佣比例输入负数被拦截  [P3]"""
        # ── 前置条件 ──
        #   运营登录后台分佣配置页面
        #
        # ── 测试步骤 ──
        #   1. 设置代理商分佣比例为-5%。
    #   2. 点击"保存"
        #
        # ── 预期结果 ──
        #   1. 输入-5。
    #   2. 提示"分佣比例不能为负数"，保存失败

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-011", title="[反向] 验证Excel缺少必要字段时导入失败", priority="P3")
    def test_fyxt_011(self, page):
        """[fyxt-011] [反向] 验证Excel缺少必要字段时导入失败  [P3]"""
        # ── 前置条件 ──
        #   准备Excel文件缺少"佣金"列，仅有用户ID和订单金额
        #
        # ── 测试步骤 ──
        #   1. 进入佣金录入页面。
    #   2. 上传缺少字段的Excel文件
        #
        # ── 预期结果 ──
        #   1. 页面正常展示。
    #   2. 提示"Excel格式错误：缺少必要字段'佣金'"，导入失败

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-012", title="[反向] 验证Excel包含不存在的用户ID时处理", priority="P3")
    def test_fyxt_012(self, page):
        """[fyxt-012] [反向] 验证Excel包含不存在的用户ID时处理  [P3]"""
        # ── 前置条件 ──
        #   准备Excel含记录：用户ID "U999"（不存在），订单金额100，佣金5
        #
        # ── 测试步骤 ──
        #   1. 上传该Excel文件。
    #   2. 查看预览和导入结果
        #
        # ── 预期结果 ──
        #   1. 上传成功，预览显示1条记录。
    #   2. 导入时提示"用户ID U999不存在"，该条记录导入失败，其他有效记录正常导入

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-013", title="[反向] 验证上传空Excel文件被拦截", priority="P3")
    def test_fyxt_013(self, page):
        """[fyxt-013] [反向] 验证上传空Excel文件被拦截  [P3]"""
        # ── 前置条件 ──
        #   准备一个只有表头没有数据的Excel文件
        #
        # ── 测试步骤 ──
        #   1. 上传空Excel文件
        #
        # ── 预期结果 ──
        #   1. 提示"文件无有效数据，请检查后重新上传"

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-014", title="[反向] 验证佣金金额为负数时导入失败", priority="P3")
    def test_fyxt_014(self, page):
        """[fyxt-014] [反向] 验证佣金金额为负数时导入失败  [P3]"""
        # ── 前置条件 ──
        #   准备Excel含记录：用户ID "U001"，订单金额100，佣金-5
        #
        # ── 测试步骤 ──
        #   1. 上传该Excel文件。
    #   2. 查看导入结果
        #
        # ── 预期结果 ──
        #   1. 上传成功。
    #   2. 提示"佣金金额不能为负数（行2）"，该条记录导入失败

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-015", title="[反向] 验证上传非Excel格式文件被拦截", priority="P3")
    def test_fyxt_015(self, page):
        """[fyxt-015] [反向] 验证上传非Excel格式文件被拦截  [P3]"""
        # ── 前置条件 ──
        #   准备一个txt文本文件
        #
        # ── 测试步骤 ──
        #   1. 点击"导入Excel"。
    #   2. 选择txt文件上传
        #
        # ── 预期结果 ──
        #   1. 文件选择弹窗出现。
    #   2. 提示"请上传.xlsx或.xls格式的文件"，上传失败

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-021", title="[反向] 验证提现金额超过可提现余额时被拦截", priority="P3")
    def test_fyxt_021(self, page):
        """[fyxt-021] [反向] 验证提现金额超过可提现余额时被拦截  [P3]"""
        # ── 前置条件 ──
        #   分销员"王五"可提现余额10元，发起提现申请50元
        #
        # ── 测试步骤 ──
        #   1. 查看"王五"的提现申请
        #
        # ── 预期结果 ──
        #   1. 该申请应在用户端被拦截提示"提现金额不能超过可提现余额10.00元"；若进入后台，运营可驳回

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-022", title="[反向] 验证代理商待结算金额为0时无法提现", priority="P3")
    def test_fyxt_022(self, page):
        """[fyxt-022] [反向] 验证代理商待结算金额为0时无法提现  [P3]"""
        # ── 前置条件 ──
        #   代理商"赵六"待结算金额为0元
        #
        # ── 测试步骤 ──
        #   1. "赵六"尝试发起提现申请
        #
        # ── 预期结果 ──
        #   1. 提示"当前无可提现金额"，无法提交提现申请

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fygz-006", title="[反向] 验证投入成本大于等于实付金额时分佣为0", priority="P3")
    def test_fygz_006(self, page):
        """[fygz-006] [反向] 验证投入成本大于等于实付金额时分佣为0  [P3]"""
        # ── 前置条件 ──
        #   一笔订单实付100元，投入成本100元，利润为0元
        #
        # ── 测试步骤 ──
        #   1. 订单完成后查看各方佣金
        #
        # ── 预期结果 ──
        #   1. 所有参与方佣金均为0.00元，不产生负数佣金

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fygz-007", title="[反向] 验证分佣比例总和超过100%利润时保存失败", priority="P3")
    def test_fygz_007(self, page):
        """[fygz-007] [反向] 验证分佣比例总和超过100%利润时保存失败  [P3]"""
        # ── 前置条件 ──
        #   运营设置平台50%、代理商40%、分销员20%，总计110%
        #
        # ── 测试步骤 ──
        #   1. 输入上述分佣比例。
    #   2. 点击保存
        #
        # ── 预期结果 ──
        #   1. 比例输入完成。
    #   2. 提示"分佣比例总和不能超过100%（当前110%）"，保存失败

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-008", title="验证分佣比例支持小数精度", priority="P4")
    def test_fyxt_008(self, page):
        """[fyxt-008] 验证分佣比例支持小数精度  [P4]"""
        # ── 前置条件 ──
        #   运营登录后台
        #
        # ── 测试步骤 ──
        #   1. 设置代理商分佣比例为3.75%。
    #   2. 保存后查看。
    #   3. 验证一笔200元订单的佣金计算
        #
        # ── 预期结果 ──
        #   1. 输入成功。
    #   2. 保存成功，显示3.75%。
    #   3. 代理商佣金为7.50元（200×3.75%）

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-016", title="验证重复导入相同数据的去重处理", priority="P4")
    def test_fyxt_016(self, page):
        """[fyxt-016] 验证重复导入相同数据的去重处理  [P4]"""
        # ── 前置条件 ──
        #   已成功导入一批佣金数据（U001佣金5元）
        #
        # ── 测试步骤 ──
        #   1. 再次上传包含相同U001记录的Excel。
    #   2. 查看导入结果
        #
        # ── 预期结果 ──
        #   1. 上传成功。
    #   2. 提示"发现重复记录（U001）"，由运营选择覆盖或跳过

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fyxt-023", title="验证结算记录导出功能", priority="P4")
    def test_fyxt_023(self, page):
        """[fyxt-023] 验证结算记录导出功能  [P4]"""
        # ── 前置条件 ──
        #   有10条结算记录
        #
        # ── 测试步骤 ──
        #   1. 进入结算管理页面。
    #   2. 点击"导出"按钮
        #
        # ── 预期结果 ──
        #   1. 页面正常展示。
    #   2. 下载Excel文件，包含结算编号、分销员/代理商名称、金额、状态、时间等字段，共10条记录

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fygz-008", title="验证利润为小数时佣金精度正确", priority="P4")
    def test_fygz_008(self, page):
        """[fygz-008] 验证利润为小数时佣金精度正确  [P4]"""
        # ── 前置条件 ──
        #   实付99.50元，投入成本62.30元，利润37.20元，代理商比例10%
        #
        # ── 测试步骤 ──
        #   1. 订单完成后查看代理商佣金
        #
        # ── 预期结果 ──
        #   1. 代理商佣金为3.72元（37.20×10%），精确到分

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")

    @case("fybc-012", title="验证餐饮人工辅助模式分佣基数确认", priority="P4")
    def test_fybc_012(self, page):
        """[fybc-012] 验证餐饮人工辅助模式分佣基数确认  [P4]"""
        # ── 前置条件 ──
        #   人工辅助商家,用户消费原价100元,折后85元
        #
        # ── 测试步骤 ──
        #   1. 订单完成
    #   2. 查看分佣计算基数
        #
        # ── 预期结果 ──
        #   1. 需确认分佣基数是原价100、折后价85、还是差额15
    #   2. 各方佣金基于确认的基数计算

        # TODO: 填写自动化代码，填完删掉下面的skip
        pytest.skip("待实现")
